# Vulgar Fraction Replacer
Simple library for replacing fractions on web pages with their unicode version, if there is one.

## Usage
```javascript
Vulgar.replace(document.body); // Or any DOM Node
```
